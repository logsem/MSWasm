(coq.theory
  (name MSWasm.iris.examples.segstack.function)
  (package coq-mswasm)
  (flags -w -notation-overridden -w -abstract-large-number -w -extraction-reserved-identifier -w -ambiguous-paths)
    (theories MSWasm MSWasm.iris.logrel MSWasm.iris.rules MSWasm.iris.examples.plainWasm))