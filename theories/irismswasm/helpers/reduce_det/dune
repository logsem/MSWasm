(coq.theory
  (name MSWasm.iris.helpers.reduce_det)
  (package coq-mswasm)
  (flags -w -notation-overridden -w -abstract-large-number -w -extraction-reserved-identifier -w -ambiguous-paths)
  (theories MSWasm MSWasm.iris.helpers.prelude))		    			   