(coq.theory
  (name MSWasm.iris.language)
  (package coq-mswasm)
  (flags -w -notation-overridden -w -abstract-large-number -w -extraction-reserved-identifier -w -ambiguous-paths)
    (theories MSWasm MSWasm.iris.helpers MSWasm.iris.language.iris))