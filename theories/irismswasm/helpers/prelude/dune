(coq.theory
  (name MSWasm.iris.helpers.prelude)
  (package coq-mswasm)
  (flags -w -notation-overridden -w -abstract-large-number -w -extraction-reserved-identifier -w -ambiguous-paths)
  (theories MSWasm MSWasm.iris.language.iris MSWasm.iris.helpers.lfill_prelude))
